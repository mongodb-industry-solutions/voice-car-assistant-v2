# atlas-app credentials — copy to .env and fill in values. Never commit .env.

# Atlas username
MONGODB_USER=

# Atlas password
MONGODB_PASS=

# Cluster hostname (e.g. cluster0.abcde.mongodb.net)
MONGODB_CLUSTER=

# Database name — same as MONGODB_DATABASE in sync-server-setup/.env
MONGODB_DATABASE=
