# Dependencies for load_documents.py — chunks + embeds the Leafy car manual and
# POSTs the chunks to the sync-enabled search-service (:8080).

sentence-transformers>=3.0.0

# voyage-4-nano's remote code (trust_remote_code) targets the transformers 4.5x
# masking API. 4.57.1 is the last release before transformers 5.x renamed
# create_causal_mask's args, which breaks the model. Do not bump without
# re-verifying the model loads and emits 1024-d vectors.
transformers==5.5.0

# CPU-only torch — avoids pulling large CUDA wheels on the host machine
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

requests>=2.31.0
