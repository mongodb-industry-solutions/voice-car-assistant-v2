# Voice Assistant Backend — REST/SSE API

# HTTP client (calls agent / navigation / telemetry / search services)
requests>=2.31.0

# Web framework + SSE
Flask>=3.0.0
Flask-CORS>=4.0.0

# Offline speech-to-text (Piper handles TTS via its own binary)
openai-whisper>=20231117
soundfile>=0.12.1

# Utilities
python-dotenv>=1.0.0
