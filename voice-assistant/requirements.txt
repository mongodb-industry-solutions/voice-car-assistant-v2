# Car Manual Voice Assistant Requirements

# HTTP Client (for search service API)
requests>=2.31.0

# AI/ML
ollama>=0.2.0  # LLM and embeddings via Ollama

# Speech Recognition
SpeechRecognition>=3.10.0  # Voice input
pyttsx3>=2.90  # Text-to-speech output
PyAudio>=0.2.14  # Microphone access (required by SpeechRecognition)

# OpenAI Whisper for offline speech recognition
openai-whisper>=20231117  # Offline STT model
soundfile>=0.12.1  # Required by Whisper for audio processing

# Utilities
python-dotenv>=1.0.0  # Environment variables (if needed)
